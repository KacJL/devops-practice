stages:
  - build
  - test
  - deploy

build-backend:
  stage: build
  image: docker:cli
  services:
    - name: docker:dind
      alias: docker
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  tags:
    - docker
  script:
    - docker version
    - docker build -t my-app ./backend

test-backend-flake8:
  stage: test
  image: python:3.12
  tags:
    - docker
  script:
    - pip install flake8
    - flake8 backend/
  needs:
    - build-backend

deploy-backend:
  stage: deploy  
  tags:
    - docker
  script:
    - echo "Deploy"
  needs:
    - job: test-backend-flake8